# StonePerms for PocketMine-MP.
#
# The plugin works with no changes to this file. Everything below is optional.

debug: false

storage:
  # "sqlite" keeps the data in this server's own file and needs nothing else.
  #
  # "mysql" points this server at a database that other servers share — the
  # same database the Endstone build opens, so a network can run both. There is
  # one set of rows, not a copy per server, so nothing is copied and nothing is
  # synchronised: each server polls a revision to learn that another changed
  # something.
  #
  # Groups, tracks and everything hanging off a group are shared. Players are
  # not: each server keeps its own.
  backend: sqlite

  # The sqlite backend only: a filename inside this plugin's data folder.
  database: stoneperms.db

  # Required by the mysql backend. Names this server, must be unique and stable
  # across the network, and selects the table holding this server's players.
  server_id: ""

  # How often to check whether another server changed something, in server
  # ticks. 20 ticks is one second.
  sync_ticks: 20

  # The mysql backend only.
  mysql:
    host: 127.0.0.1
    port: 3306
    database: stoneperms
    username: stoneperms
    password: ""
    connect_timeout: 5
    # Kept so one config file serves both plugins. The PocketMine driver has no
    # read timeout of its own and ignores this.
    read_timeout: 10
    # Path to a CA certificate to require TLS on the connection.
    ssl_ca: ""

permissions:
  # Group every player receives automatically.
  default_group: default

contexts:
  server: global
  include_device_os: false
  include_locale: false

maintenance:
  # How often expired permissions are checked, in server ticks.
  expiry_check_ticks: 20
  # How often the PocketMine permission list is refreshed, in server ticks.
  catalog_check_ticks: 100

startup:
  # Show the full StonePerms status block after the plugin has loaded.
  show_summary: true
  # Warn when server settings limit player identity or conflict with chat formatting.
  check_server_properties: true

web:
  # Enables the connection from this server to the web API.
  # "/stoneperms web pair <code>" turns this on for you.
  enabled: false
  # Official service. Replace this address only when using a self-hosted StonePerms API.
  api_url: "https://stoneperms.spindexgfx.com"
  server_id: ""
  server_token: ""
  server_name: "PocketMine-MP server"

display:
  chat:
    # Turn this off if another plugin already formats chat.
    enabled: false
    format: "{prefix}§f{name}{suffix} §8» §f{message}"
  nametag:
    # Text displayed above each online player.
    enabled: false
    format: "{prefix}§f{name}{suffix}"
